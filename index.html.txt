<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8" />
  <title>Live GPS Location</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <style>
    body { font-family: Arial, sans-serif; padding: 20px; }
    button { padding: 12px 16px; font-size: 16px; cursor: pointer; }
    pre { background: #f5f5f5; padding: 10px; }
  </style>
</head>
<body>
  <h2>📍 Live GPS Location</h2>
  <p>Button dabao → Browser me Allow Location karo</p>

  <button onclick="getLocation()">📍 Get My Location</button>

  <pre id="out">Coordinates yahan dikhenge...</pre>

  <script>
    function getLocation() {
      if (!navigator.geolocation) {
        alert("Your browser does not support GPS");
        return;
      }

      navigator.geolocation.getCurrentPosition(
        (pos) => {
          const lat = pos.coords.latitude;
          const lon = pos.coords.longitude;
          document.getElementById("out").innerText =
            `Latitude: ${lat}\nLongitude: ${lon}`;
        },
        (err) => {
          alert("Location permission denied. Please allow GPS access.");
          console.error(err);
        },
        { enableHighAccuracy: true, timeout: 10000 }
      );
    }
  </script>
</body>
</html>